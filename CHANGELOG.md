# 更新日志

版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。`manifest.json` 里的版本号只在真正发布时才改
（Chrome 只认纯数字的版本号，写不下 `-dev` 这类后缀），开发期间以本文件的「未发布」一节为准。

## 未发布

## 1.0.0 —— 能交给别人用

工程外围和界面语言补齐，可以打包上架了。

### 结构

- **不再手抄常量**：加 esbuild 打包步骤，内容脚本改成 `content/src/index.js` 直接 import
  `common.js`。DEFAULTS 曾经在三个文件里各有一份，靠人记着同步 —— 忘一处就是「设置页
  显示的和实际生效的不一样」。开发时加载 `dist/`。
- **content.js 拆成六个模块**（segments / scheduler / cache / tracks / overlay / bridge，外加
  一个 state），行为一字未改，620 条断言原封不动全绿，而且跑的是打包产物。
- **切句有了不需要假 DOM 的直接单测**，这是拆分换来的第一样东西。

### 界面

- **双语（简体中文 / English）**，跟随浏览器语言。英文包 213 条，漏没漏由
  `test/i18n.test.js` 机械对账，`popup.test` 再接上真的语言包跑一遍。
- 模型候选换成 `gpt-5.6-luna` / `deepseek-v4-flash`，输入框仍然自由填写。
- 设置页页脚的版本号从 manifest 读 —— 它曾经写死在 v0.1.0 上，一路发到 0.3.0 都没人发现。

### 发布

- **端到端冒烟**：真 Chrome 装真扩展，假 YouTube 假接口，跑冷启动 / 切视频 / 换轨 / 限流四条。
  单元测试全绿也可能根本装不起来，这四条堵的就是那个洞。
- `STORE.md`：商店文案、隐私说明、权限说明、上架前的核对清单。
- `npm run shots` 生成弹窗和设置页的截图；字幕框那张要在真视频上重拍。
- `npm run keygen` 可以把扩展 ID 定死。**没有默认打开**：它会当场换掉 ID，现有的设置和
  已经付过钱的译文缓存从此读不到 —— 该由你挑时机，而不是在一次升级里悄悄发生。

### 修

- scheduler 里一个叫 `t` 的计时器变量把取文案的 `t()` 遮住了（只有超时那条分支会走到，
  在浏览器里才炸）。为此给扩展本体开了 `no-shadow`。
- 「重翻本视频」的删除有可能被在途的落盘写回来 —— 删除也排进 background 那条队列了。

## 0.3.0 —— 让问题看得见

上一版把会花钱、会卡死的路径堵上了；这一版解决「出了问题没人知道为什么」。

- **错误有了九种码**（noKey / noPerm / auth / model / rate / server / timeout / format / network）。
  弹窗按码说人话并给出唯一的那个下一步：去设置 / 去授权 / 重试 / 什么都不用做。
  服务商的原话挂在 title 上留着，报障时唯一能定位的往往就是它。
- **YouTube 改版会自检**。读不到 player response 是致命的，弹窗直说「YouTube 改版了」
  并列出缺的是哪一项；captions / 音轨 / 控制栏这些缺了只是残一点，照常翻，留在诊断信息里。
- **首次安装直接打开设置页**；Key 为空时弹窗顶上挂一条「还没配置接口 → 去设置」。
- **「复制诊断」**：版本、UA、当前配置、页面状态、两边各 200 条日志，一键拿走。
  里面绝不含 API Key（只写「已填 / 空」），接口只留主机名，原文译文一个字不带。
- **页面内消息带随机 token**：页面脚本再也伪造不出一条假字幕塞进来。
- **关页面前的译文不再丢**：落盘防抖降到 1.5 秒，pagehide 和转入后台也触发。
  顺带修掉「重翻本视频」的删除被在途落盘写回来的竞态。
- 「这个视频没有英文字幕」改成写真正识别出来的那个语言。

## 0.2.0 —— 止血

这一版一条新功能都没有，全部用来修「会花冤枉钱」和「会把人卡死」的路径。

### 不再花冤枉钱

- **切推理档位不再作废整片译文。** 它以前既在 OUTPUT_KEYS 里又在缓存键里，切一下就是
  全片清空 + 换一把缓存键，重新买一遍 —— 而 README 推荐的用法正是「难懂的段落临时开中档」。
  现在只影响之后发出的批次。缓存签名里那一格钉死在老默认值上，已存的缓存照常命中。
- **切视频 / 换字幕轨会真的取消在途请求。** 以前只是「结果不采用」，background 那边的
  fetch 照跑，跑完还接着 strict 重问、repair 补翻、错位拆块。并发 3 时一次切视频最多
  白付十来次请求，还占着下一个视频的并发额度。
- **两个标签页看同一个视频，缓存不再互相覆盖。** 正文写入改走 background 的队列做合并，
  内容脚本只发增量，结果是并集（索引早就这么做了，正文一直没跟上）。

### 不再把人卡住

- **限流有了全局退避。** 按接口地址记冷却，撞上 429 的批次自己排队、自己回来，状态写
  「接口限流，N 秒后自动重试」，不必人去点重试。等多久优先听 Retry-After（秒数和 HTTP
  日期都认，封顶 30 秒），没给就 2 / 4 / 8 秒指数退避；自动重试封顶 5 次。
- **后台没有回应时有客户端兜底。** MV3 的 service worker 空闲会被回收，回收发生在响应
  回来之前的话这一批会永久停在「翻译中」，并发额度从此少一个，攒够三次整个视频停摆。
  现在 120 秒到点就把额度还回来并报错。
- **直播 / Shorts / 没有播放器的页面直说不支持**，不再永远转圈。判据用的是「正在直播」
  而不是「曾经是直播」—— 直播完再挂上来的录播照常翻。

### 工程

- 加 `package.json`：`npm test` / `npm run lint` / `npm run zip` / `npm run check`。
- 加 eslint 最小规则集（只抓「一定是错」的问题，不管风格），修掉它报出来的 9 处。
- 加 `tools/zip.mjs`：零依赖打包，产出 `dist/sub-translator-<版本>.zip`，白名单进包。
- 加 GitHub Actions：push / PR 上跑 lint + 测试 + 打包，包作为构建产物留存。

## 0.1.0

第一个能用的版本：自动识别原声语言、按播放头懒翻译、双语字幕框（可拖动 / 可调宽 / 可选中复制）、
本地缓存、接口配置档、自带译文轨采用、批次自动升降档、对齐容错与统计。
