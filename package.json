{
  "name": "sub-translator",
  "version": "0.2.0-dev",
  "private": true,
  "description": "YouTube 双语字幕翻译 Chrome 扩展",
  "scripts": {
    "test": "node test/run.js",
    "lint": "eslint .",
    "zip": "node tools/zip.mjs",
    "check": "npm run lint && npm test"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0"
  },
  "engines": { "node": ">=18" }
}
